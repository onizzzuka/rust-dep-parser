Parser of dependencies files for Rust projects. Accept .json, .toml and .lock files.
The files should be placed into the `files_for_parse` directory.

Output will be placed into the `output` directory.

## Usage

1. Run `composer install` for install dependencies.
2. Run `php parser.php`.
